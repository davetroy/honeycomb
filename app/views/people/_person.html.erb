<div class="appearance">
  <%=link_to image_tag(person.image), person_url(person)%><br/>
  <%=truncate(person.show_name, :length =>25)%><br/>
  <%=person.devices.size%> devices<br/>
  <%="That's You!" if person.id==session[:person_id]%>
</div>

